# Library-Management-System
A simple Library Management System in C using file handling. Features: add books, view list, search by ID, issue and return books. Data is saved in books.dat. Compile with gcc main.c -o library and run ./library. Perfect beginner project for learning structs, files and menus.
